#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

/* The function printing the string using printf */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
/* Returning 0 for success */
return (0);
}
