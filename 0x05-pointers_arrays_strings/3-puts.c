#include "main.h"
#include <stdio.h>

/**
 * _puts - print string with stdout
 *@str: String
 * Description:  prints a string, followed by a new line
 * Return:No return
 */

void _puts(char *str)
{
fprintf(stdout, str);
}
